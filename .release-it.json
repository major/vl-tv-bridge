{
  "git": {
    "commitMessage": "Release v${version}",
    "tagName": "v${version}",
    "push": true,
    "requireBranch": "main",
    "requireCleanWorkingDir": true
  },
  "github": {
    "release": true,
    "releaseName": "v${version}",
    "releaseNotes": "## VL TradingView Bridge v${version}\n\n### Installation\n- **Firefox (Signed)**: Download the `-signed.xpi` file - this is signed by Mozilla and will install without warnings\n- **Source Code**: Download the `.zip` for review or manual installation\n\n### Files\n- `vl-tv-bridge-${version}-signed.xpi` - Mozilla-signed extension (recommended)\n- `vl-tv-bridge-${version}.zip` - Source code\n\n### Changes\nSee commit history for details.",
    "assets": [
      "build/vl-tv-bridge-*.zip",
      "build/vl-tv-bridge-*-signed.xpi"
    ]
  },
  "hooks": {
    "before:init": ["npm run lint"],
    "after:bump": [
      "node -e \"const fs=require('fs');const pkg=require('./package.json');const manifest=require('./firefox/manifest.json');manifest.version=pkg.version;fs.writeFileSync('./firefox/manifest.json',JSON.stringify(manifest,null,2)+'\\n')\"",
      "git add firefox/manifest.json"
    ],
    "before:github:release": ["npm run build"],
    "after:release": "echo 🎉 Release ${version} published to GitHub!"
  },
  "npm": {
    "publish": false
  },
  "plugins": {
    "@release-it/conventional-changelog": {
      "preset": {
        "name": "conventionalcommits"
      },
      "infile": "CHANGELOG.md"
    }
  }
}
